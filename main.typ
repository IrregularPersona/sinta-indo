// Heading
= Level 1 Heading
== Level 2 Heading
=== Level 3 Heading

#linebreak()

// Bullet List
- Hello
- There
  - What
  - Is
    - Going
    - On
      - Hello there :D

// Numbered List
+ Numbered
+ List
  + Inner
  + Numbered
    + List
      + Pretty cool in my opinion

// Markdown basics!
_italics_

*bolds*

Here are those --- ChatGPT lines :D // Emdashes

// Term Lists
Theres also this thing called a Term List

/ Some Term: It means something apparently
/ Box: A 3 Dimensional Square!

// Math Mode! (One of their more impressive achievements)

$x + y = 25$ // This initializes math mode

$"Some String Literal!"$ // You can use Quotes for text literals1

$2 pi r^2 arrow "Something for a circle"$ // You can get Mathematic symbols directly!

$ x + y = 25 $ // Now lets create some math blocks!

$ (-b plus.minus sqrt(b^2 - 4 a c))/ (2 a)  $ // Quadratic Formula!

// One interesting thing about Typst is the syntax highlighting by default for code blocks

```rs
mod some_module;

fn main() {
  println!("Hello, World!");
}
```

// There are also links, which auto embeds them. Thats it.
https://google.com/

